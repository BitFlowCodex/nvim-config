## How to use
First make sure your ~/.config doesnt have an nvim folder, if it does, delete it 
then use the following command:

```bash
git clone https://github.com/BitFlowCodex/nvim-config.git ~/.config/nvim 

``` 
